###############################################
# ACTA Demo — Environment Variables (Example) #
###############################################

# Base URL of the ACTA API you will integrate with.
# Use the local API when running the ACTA API locally.
# Examples:
# - Local: http://localhost:8000
# - Remote (testnet): https://api.acta.build
NEXT_PUBLIC_ACTA_API_URL=http://localhost:8000

# PostHog analytics keys and host. Required to enable analytics.
NEXT_PUBLIC_POSTHOG_KEY=
NEXT_PUBLIC_POSTHOG_HOST=

# Soroban RPC URL to submit XDR transactions and read chain state.
# Default testnet RPC:
NEXT_PUBLIC_SOROBAN_RPC_URL=https://soroban-testnet.stellar.org

# Stellar network passphrase used for signing and verifying.
# Testnet default:
NEXT_PUBLIC_NETWORK_PASSPHRASE=Test SDF Network ; September 2015

# Contract ID of the ACTA issuance contract (public network address).
# Replace with your deployed contract ID on the desired network.
NEXT_PUBLIC_ACTA_ISSUANCE_CONTRACT_ID=CANYEUDJCAPQ5ACXXJQXR4VA6727LFGFP2FFE35MF3YEQTXCMIA7BNWA

# Contract ID of the ACTA vault contract (public network address).
# Replace with your deployed contract ID on the desired network.
NEXT_PUBLIC_VAULT_CONTRACT_ID=CD7AN2XKCQLFNENL6YUUNZ6FBAL63N5J5X7AEGLRYSG6YBS6V35OSJCH

# WalletConnect (servidor; NO público)
# Configura el Project ID de WalletConnect para uso desde el servidor.
# Copia este archivo a .env.local y establece el valor real.
WALLETCONNECT_PROJECT_ID=

# Notes:
# - Variables prefixed with NEXT_PUBLIC_ are exposed to the client and
#   must NOT contain secrets.
# - To use custom values, copy this file to .env.local and edit.
# - When switching between networks (testnet/mainnet), update RPC URL,
#   passphrase, and contract IDs accordingly.